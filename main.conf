{
	instances = {
		list = Template(f"templates/list.html");

		signin = PostFieldReader(
			)

		content = Dir() {
			main->instances.list;
			signin->instances.signin;
		};

		begin = Dir() {
			default->Template(f"templates/skelet.html") {
				content->instances.content;
			};
			static->Fileserver(f"static|png,js,jpg,zip,gif");
		};
	};
}