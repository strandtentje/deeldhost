{
	instances = {
		creds = Connector(
			"Server=localhost; Database=deeldhost; User ID=deeldhost; Password=deeldhost; Pooling=true; Allow User Variables=True;");

		begin = Dir() {
			default->HttpStatus(mime = "text/html") {
				next-><f"calls.conf@begin">(locale = "en.html");
			};
			api->HttpStatus(mime = "application/javascript") {
				next-><f"calls.conf@begin">(locale = "en.json");
			};
			static->Fileserver(f"static|png,js,jpg,zip,gif");
		};
	};
}